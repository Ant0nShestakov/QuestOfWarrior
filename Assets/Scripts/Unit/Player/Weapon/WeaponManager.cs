using UnityEngine;

public class WeaponManager : MonoBehaviour
{
    //private UnitModel _playerModel;

    //private void Start() => _playerModel = GetComponent<PlayerModel>();
    
    //private void OnTriggerEnter(Collider other)
    //{
    //    if (other.TryGetComponent<IDamageable>(out IDamageable enemy))
    //    {
    //        if (enemy as PlayerModel)
    //            return;

    //        enemy.ApplyDamage(_playerModel.Damage);
    //    }
    //}
}
